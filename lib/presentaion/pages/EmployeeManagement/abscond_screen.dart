import 'package:flutter/material.dart';

class AbscondSscreen extends StatelessWidget {
  const AbscondSscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Employees on Leave",
        style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
      ),
    );
  }
}
